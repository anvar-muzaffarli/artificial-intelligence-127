import React from 'react';
import ReactDOM from 'react-dom/client';
// named export adlandirilmish cole cixarilma
import Proqram from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Proqram />
    
  </React.StrictMode>
);


